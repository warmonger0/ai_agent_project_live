# /home/war/ai_agent_project/pytest.ini

[pytest]
addopts = 
    --tb=short 
    -p no:warnings
    -p pytest_asyncio

# ✅ Tell pytest where the test files live
testpaths = 
    backend/app/tests/unit
    backend/app/tests/integration

# ✅ Fix import resolution for app.*
pythonpath = backend
